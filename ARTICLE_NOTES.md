# 着席検知システム 開発記録（記事用ノート）

古いAndroidスマホ（Termux）で着席・離席を自動検知するシステムを作った記録。
苦労した点・試行錯誤・変更の経緯を時系列でまとめておく。

---

## システム概要（最終形）

- デバイス: OPPO Android 15（ColorOS）+ Termux
- 検知方法: 30秒ごとに重力センサーを1点取得、前回との差分（|ΔX|+|ΔY|+|ΔZ|）で判定
- 通知先: Discord Webhook（リアルタイム + 日次サマリー + バッテリーアラート）
- 管理: PC側でコード編集 → GitHub push → スマホ側 git pull のみ

---

## 苦労ポイント一覧

### 1. センサー取得方式の迷走

**最初の実装:** `termux-sensor -s gravity` をPopenで連続ストリームとして流し続ける方式

**問題:**
- プロセスが溜まっていく
- 停止するために SIGINT / pkill が必要
- pkill を使うと Termux の binder（内部通信基盤）が壊れてセンサーが二度と動かなくなった
- 復旧するには `termux-sensor -c`（クリーンアップ）、それでもダメなら Termux:API をGUIから強制停止する必要があった

**解決策:** `-n 1` オプションで1点取得して自然終了させる方式に変更
```bash
termux-sensor -s gravity -n 1
```
これならプロセスは自然終了するので SIGINT / pkill が一切不要になった。

**教訓:** Android の binder は一度壊れると再起動なしには戻せない。pkill は絶対NG。

---

### 2. 着席確定バグ（永遠に着席にならない）

**症状:** 明らかに着席しているのに `[着席]` 通知が来ない

**原因:** 初期実装では「着席スパイクが2回連続来たら確定」というロジックだった。
重力センサーの差分スパイクは2〜3分に1回程度しか来ないため、2回連続は現実的に発生しなかった。

**変更前:**
```
スパイク1回目 → 着席候補
スパイク2回目（連続）→ 着席確定
```

**変更後:**
```
スパイク1回 → 即着席確定（1発で十分）
```

**教訓:** センサーの実際の挙動を確認せずに「2回連続」という条件を設けたのが間違い。実測してみると1発のスパイクで十分だった。

---

### 3. 画面オン問題（最も苦労した箇所）

**背景:** Android 15 + OPPO ColorOS は Termux がバックグラウンド（画面オフ）だとセンサーにアクセスできない。画面を常時オンにする必要がある。

**試したこと（全部失敗）:**

| 試した方法 | 結果 | 理由 |
|-----------|------|------|
| Termuxから `settings put system screen_off_timeout` | 権限エラー | Termuxにはsystem settings書き込み権限なし |
| `adb connect 127.0.0.1:5555` | Connection refused | Android 11以降、USB未接続ではTCPポート5555が開かない |
| ワイヤレスデバッグでペアリング | エラー繰り返し | ADBバージョン互換性の問題で迷走 |
| WSL2に `sudo apt install adb` | パスワードでブロック | WSL2はsudoにパスワードが必要 |
| Linux版 platform-tools を手動DL | unzipなし → Python展開 → 権限エラー | chmod +xで解消したが遠回り |

**解決策:** Windows側のADBを使う
```
C:\Users\zabie\AppData\Local\Android\Sdk\platform-tools\adb.exe
```
Android Studio のSDKに既にあった。WSL2からこれを呼び出す。

**画面オフの2段階問題:**
1. `screen_off_timeout` を最大値にしても1分でオフになる
2. OPPO独自の省電力設定 `power_save_screenoff_time_state` が別途存在していた
   → `adb shell settings get system` を全部grepして発見

```bash
$ADB shell settings put system screen_off_timeout 2147483647    # タイムアウト無効
$ADB shell settings put system power_save_screenoff_time_state 0 # OPPO省電力オフ
```

**さらにScreenKeeperを追加（念のため）:**
Termux側から `adb connect localhost:5555` → 25秒ごとに `KEYCODE_WAKEUP` を送信するスレッドを実装。
ColorOSの近接センサー（裏返し消灯）に対する保険。

**教訓:** 
- USBデバッグとワイヤレスデバッグで迷走しないこと。USBで十分。
- WSL2はUSBを直接認識しない。Windows側の.exeをWSL2から呼ぶのが正解。
- OPPOはsettingsキーが独自のものが多い。標準の設定を変えても効かない場合は全キーをgrepして探す。

---

### 4. バッテリー取得の失敗

**最初の実装:** `termux-battery-status`（Termux:API経由）

**問題:** Termux:API の binder 経由での取得が頻繁に失敗する。センサーと同じbinderを使うため、センサー取得と干渉する可能性があった。

**解決策:** `/sys/class/power_supply/battery/capacity` の直読みに変更
```python
with open("/sys/class/power_supply/battery/capacity") as f:
    return int(f.read().strip())
```
このファイルは権限 0444（world-readable）で、Termuxユーザーからも読める。SELinuxも拒否なし。

**フォールバック構成（4段階）:**
1. `/sys/class/power_supply/battery/capacity`（直読み・最速）
2. `/sys/class/power_supply/mtk-battery/capacity`（MediaTek系）
3. `/system/bin/cmd battery get level`
4. `termux-battery-status`（最終手段・15秒タイムアウト付き）

**教訓:** Termux:API は便利だが binder 依存で不安定。sysfs直読みの方が確実で高速。

---

### 5. 閾値の迷走（delta threshold）

**検知ロジック:** 前回と今回の重力センサー値の差分 `|ΔX|+|ΔY|+|ΔZ|` を使う

**変遷:**

| 時期 | 閾値 | 変更理由 |
|------|------|---------|
| 初期 | 0.03 | とりあえずの値 |
| → 0.07 | 机の上のスマホを手で触っただけで delta=0.063 が出て誤着席確定していた |
| → 0.05 | 0.07は高すぎた。静止着席中（PCで集中作業中）の実測最大値が0.038しかなく、5分間全部「離席判定」になって誤離席が頻発した |

**実測データ（静止着席中・5分間）:**
```
0.006, 0.012, 0.010, 0.006, 0.008, 0.011, 0.018, 0.019, 0.038, 0.002
```
最大値でも 0.038。0.07 には全然届かない。

**教訓:** センサーの「着席中の静止状態」と「離席状態」はほぼ区別できない。閾値を下げても「本当に誰もいない机の上」と区別できないため、根本的にはdebounce時間で対応するしかない。

---

### 6. 離席判定の誤作動（debounce時間）

**症状:** 集中して作業中（全く動いていない）に `[離席]` 通知が来る

**原因:** delta < 閾値 が5分（300秒）継続したら離席確定、というロジック。
PCで集中作業中は体がほぼ静止するため、スマホの重力センサーにも動きが伝わらない。

**変更:** `DEBOUNCE_LEFT_SEC` を 300秒（5分）→ 600秒（10分）に延長

**トレードオフ:**
- 本当に離席したとき、10分後でないと通知が来なくなる
- ただし実際の外出・トイレ等は10分以上になることが多いので許容範囲

---

### 7. 通知の進化

**初期:** シンプルな文字列のみ
```
[着席] 2026-05-09 10:23:45 作業を開始しました。
```

**バッテリー追加（2026-05-09）:**
```
[着席] 2026-05-09 10:23:45 作業を開始しました。  🔋82%
```

**Δ値追加（2026-05-09）:**
```
[着席] 2026-05-09 10:23:45 作業を開始しました。  🔋82%  Δ: 0.696
[離席] 2026-05-09 12:47:01 席を離れました。  🔋79%  Δmax: 0.038
```

Δ値を追加した理由: 誤検知かどうかを後から判断できるようにするため。
- `[着席]` の `Δ:` → 着席確定を引き起こしたスパイク値（大きいほど確実な着席）
- `[離席]` の `Δmax:` → 待機中（10分間）の最大値（小さいほど静止＝誤離席の可能性あり）

---

### 8. 機能の追加史

**日次サマリー（2026-05-08追加）:**
毎日22:00に当日の着席時間を集計してDiscordに送信。

```
📊 着席サマリー 5月9日（金）
09:15 〜 11:45  （2時間30分）
13:00 〜 15:30  （2時間30分）
合計着席: 5時間0分
```

**手動トリガーエンドポイント（2026-05-09追加）:**
22:00を待たずに手動で送信できる Flask エンドポイント。
```bash
curl -X POST http://192.168.10.104:8080/api/summary/send
```

**バッテリーアラート（2026-05-09追加）:**
20% / 15% / 10% を下回ったときにサマリーchに通知。
着席・離席確定時 + 30分ごとにチェック（最初は30秒ごとに実装してしまい「やりすぎ」と修正）。

---

## コミット一覧

| コミット | 内容 |
|---------|------|
| `a32f6d1` | 日次サマリー追加 |
| `233485c` | 着席確定バグ修正（1スパイクで即確定） |
| `ba5de3a` | バッテリー通知・画面設定の安定化 |
| `03aa183` | バッテリー取得多段フォールバック |
| `fa9c138` | ScreenKeeper追加 |
| `6497a94` | VARIANCE_THRESHOLD 0.03→0.07 |
| `d416c4c` | 着席・離席通知にバッテリー残量追加 |
| `7244f76` | POST /api/summary/send エンドポイント追加 |
| `da21e64` | バッテリーアラート20%/15%/10%追加 |
| `2acda9c` | バッテリーアラートを30分+着席/離席時のみに変更 |
| `a353186` | 閾値0.07→0.05・debounce 300→600秒・通知にΔmax追加 |
| `0ff4ebd` | CLAUDE.md更新・IDEAS.md追加 |

---

## 今後のアイデア（IDEAS.md 参照）

1. 着席時間の目標管理・進捗通知（30分ごとに累積時間と達成率をDiscord通知）
2. 着席時に音楽を自動再生（`termux-media-player play`）

